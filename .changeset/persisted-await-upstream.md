---
"@tanstack/db-sqlite-persistence-core": minor
---

Add an `upstreamLoadSubsetMode` option to persisted collection options so sync-present persisted collections can await upstream subset loading after hydrating local SQLite rows.
